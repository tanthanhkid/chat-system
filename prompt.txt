
Prompt 1: ở cả chat widget và admin interface chat windows bên dưới tin nhắn gửi đi thì có hiển thị trang thái đã xem hay chưa để người gửi biết người đọc đã xem.

Prompt 2:
- Sử dụng shadcn cho admin interface để áp dụng được cho theme bên dưới.
- sử dụng shadcn theme: npx shadcn@latest add https://tweakcn.com/r/themes/northern-lights.json 


Hãy fix lỗi trước sau đó dùng selenium viết automation test cho các chức năng này, test và fix đến khi nào hết lỗi thì mới được phép dừng, think hard.
nếu cần sửa database thì xoá docker container cũ và chạy docker compose để khởi tạo lại từ init.sql script